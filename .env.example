# Copy this file to .env and customize the secrets before running docker compose.
# These values are read by docker/docker-compose.observability.yml.

MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=change-me
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change-me
